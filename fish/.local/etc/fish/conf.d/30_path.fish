set -e PATH

set PATH
set -a PATH "/sbin"
set -a PATH "/bin"
set -a PATH "/usr/sbin"
set -a PATH "/usr/bin"
set -a PATH "/usr/local/bin"
set -a PATH "$HOME/.bin"
set -a PATH "$HOME/.local/bin"

set -x PATH
